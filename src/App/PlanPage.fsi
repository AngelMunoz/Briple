module App.PlanPage

// The Plan page (storyboard 4.5): the file's title and origin, every variant
// as a row, the anchor row with the re-anchor roller, the #x extras as
// expander sections, and the imports history. The shell owns the app bar
// (export and remove). The phone layout is the design of record.

open Fable.Ripple.Dom

val view: unit -> DomItem
